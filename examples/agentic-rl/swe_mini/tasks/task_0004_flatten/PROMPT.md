# Task: fix `flatten`

`bug.py` defines `flatten(items)`. It should turn an arbitrarily nested list into
a single flat list, left to right — `flatten([1, [2, [3]]]) == [1, 2, 3]`.
Non-list values (including strings) are kept as-is.

Read `bug.py`, fix it, run the tests, and submit when `test_task.py` passes.
