# Task: fix `fib`

`bug.py` defines `fib(n)`, the 0-indexed Fibonacci sequence where `fib(0) == 0`
and `fib(1) == 1`. It is wrong for at least one input.

Read `bug.py`, fix it with a minimal edit, run the tests, and submit when
`test_task.py` passes.
