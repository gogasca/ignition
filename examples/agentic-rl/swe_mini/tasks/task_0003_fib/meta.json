{
  "task_id": "task_0003_fib",
  "summary": "wrong base case: returns 1 for n<2 instead of n",
  "solution": {
    "path": "bug.py",
    "content": "def fib(n):\n    \"\"\"Return the n-th Fibonacci number (0-indexed): fib(0)=0, fib(1)=1.\"\"\"\n    if n < 2:\n        return n\n    a, b = 0, 1\n    for _ in range(n - 1):\n        a, b = b, a + b\n    return b\n"
  }
}
