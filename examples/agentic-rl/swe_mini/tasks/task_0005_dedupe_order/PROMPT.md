# Task: fix `dedupe`

`bug.py` defines `dedupe(xs)`. It should remove duplicates **while keeping the
order of first appearance** — `dedupe([3, 1, 3, 2, 1]) == [3, 1, 2]`.

Read `bug.py`, fix it, run the tests, and submit when `test_task.py` passes.
