# Task: fix `sum_list`

`bug.py` defines `sum_list(xs)`, which should return the sum of every number in
`xs` (and `0` for an empty list). It currently returns the wrong total for most
inputs.

Read `bug.py`, fix the bug with a minimal edit, run the tests, and submit when
`test_task.py` passes.
