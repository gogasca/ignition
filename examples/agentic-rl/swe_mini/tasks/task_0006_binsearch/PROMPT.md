# Task: fix `binary_search`

`bug.py` defines `binary_search(arr, target)` over a sorted list. It should
return the index of `target`, or `-1` if it is not present. It currently misses
some elements (for example the largest, or the only element of a 1-element list).

Read `bug.py`, fix it with a minimal edit, run the tests, and submit when
`test_task.py` passes.
