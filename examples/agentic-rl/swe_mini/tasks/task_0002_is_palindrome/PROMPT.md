# Task: fix `is_palindrome`

`bug.py` defines `is_palindrome(s)`. It should return `True` when `s` reads the
same forwards and backwards **ignoring case, spaces, and punctuation** — so
`"A man, a plan, a canal: Panama"` is a palindrome.

Read `bug.py`, fix it, run the tests, and submit when `test_task.py` passes.
